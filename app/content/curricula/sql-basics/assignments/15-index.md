以下の問いに答えてください。SQLは実際に動くものを記述し、説明問題は日本語で答えてください。

---

1. `orders` テーブルの `user_id` カラムにインデックスを作成するSQLを書いてください。また、そのインデックスを削除するSQLも書いてください。

2. 以下の2つのカラムについて、インデックスを作成することが効果的かどうかをそれぞれ理由とともに説明してください。
   - `users.email`（全ユーザーでほぼ重複しないユニークな値）
   - `users.role`（'admin' / 'instructor' / 'learner' の3種類のみ）

3. `EXPLAIN` または `EXPLAIN ANALYZE` を使ってインデックスの効果を確認する手順を説明してください。インデックスありとなしで出力結果のどの部分に違いが出るか述べてください。
